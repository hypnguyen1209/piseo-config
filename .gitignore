# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# Secrets — KHÔNG BAO GIỜ commit các file này
auth.json
daemon-keypair.json
*.key
*.pem

# node_modules được cài lại bằng `bun install` từ package-lock.json
pi/packages/node_modules/
